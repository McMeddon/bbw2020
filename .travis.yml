language: node_js
node_js:
    - "8.1.2"

before_script:
  - npm install firebase-tools -g

script:
  - firebase deploy --only hosting --token $FIREBASE_TOKEN --bbw2020-fe79b --only-hosting
  - echo "Neue Runde neues Glück! Deploying!"

branches:
  only:
    - master

after_success:
    message: "jawollo!"

