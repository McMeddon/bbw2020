language: node_js
node_js:
    - "12"
branches:
    only:
        - master

before_script:
    -npm install -g firebase-tools

script:
    - echo "Neue Runde neues Glück! Deploying!"
after_success:
    - firebase deploy --token $FIREBASE_TOKEN --bbw2020-fe79b --non-interactive

